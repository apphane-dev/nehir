---
"nehir": patch
contributors: [yougotwill]
---

Focus Previous Window (Cmd-Option-Tab) now returns to the globally most-recently-focused window even when it lives on a different workspace or monitor, instead of only searching the current workspace. Fixes #92.
