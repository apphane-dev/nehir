---
"nehir": patch

---

Preserve the Niri viewport during mouse-driven focus changes. When focus-follows-mouse is enabled, hovering a window can update focus and selection without recentring or scrolling the viewport to bring that selected window fully into view, so users keep their manually positioned viewport while moving the pointer across visible columns.
