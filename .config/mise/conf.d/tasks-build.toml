[tasks."build"]
description = "Build Nehir (debug)"
run = "swift build"

[tasks."build:release"]
description = "Build Nehir (release, optimized)"
run = "swift build -c release"

[tasks."package:release"]
description = "Package Nehir.app from a release build"
run = "Scripts/package-app.sh release"
